import { FilterByTrafficTypePipe } from './filter-by-traffic-type.pipe';

describe('FilterByTrafficTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByTrafficTypePipe();
    expect(pipe).toBeTruthy();
  });
});
