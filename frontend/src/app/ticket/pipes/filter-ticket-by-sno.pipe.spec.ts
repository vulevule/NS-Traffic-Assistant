import { FilterTicketBySnoPipe } from './filter-ticket-by-sno.pipe';

describe('FilterTicketBySnoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTicketBySnoPipe();
    expect(pipe).toBeTruthy();
  });
});
