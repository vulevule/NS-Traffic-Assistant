import { FilterTicketByZonePipe } from './filter-ticket-by-zone.pipe';

describe('FilterTicketByZonePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTicketByZonePipe();
    expect(pipe).toBeTruthy();
  });
});
