import { FilterByLinePipe } from './filter-by-line.pipe';

describe('FilterByLinePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByLinePipe();
    expect(pipe).toBeTruthy();
  });
});
