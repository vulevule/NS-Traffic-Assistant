import { FilterByStationPipe } from './filter-by-station.pipe';

describe('FilterByStationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByStationPipe();
    expect(pipe).toBeTruthy();
  });
});
