import { FilterByZonePipe } from './filter-by-zone.pipe';

describe('FilterByZonePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByZonePipe();
    expect(pipe).toBeTruthy();
  });
});
