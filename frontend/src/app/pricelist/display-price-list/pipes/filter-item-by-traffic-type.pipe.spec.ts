import { FilterItemByTrafficTypePipe } from './filter-item-by-traffic-type.pipe';

describe('FilterItemByTrafficTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterItemByTrafficTypePipe();
    expect(pipe).toBeTruthy();
  });
});
