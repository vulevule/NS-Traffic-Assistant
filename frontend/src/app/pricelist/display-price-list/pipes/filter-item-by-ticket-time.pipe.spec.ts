import { FilterItemByTicketTimePipe } from './filter-item-by-ticket-time.pipe';

describe('FilterItemByTicketTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterItemByTicketTimePipe();
    expect(pipe).toBeTruthy();
  });
});
