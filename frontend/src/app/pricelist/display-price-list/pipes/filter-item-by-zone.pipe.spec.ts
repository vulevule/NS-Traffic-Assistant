import { FilterItemByZonePipe } from './filter-item-by-zone.pipe';

describe('FilterItemByZonePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterItemByZonePipe();
    expect(pipe).toBeTruthy();
  });
});
