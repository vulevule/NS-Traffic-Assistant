import { MatchingPasswordDirective } from './matching-password.directive';

describe('MatchingPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchingPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
